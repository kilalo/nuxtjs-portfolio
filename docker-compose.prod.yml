services:
  portfolio-prod:
    image: ghcr.io/kilalo/nuxtjs-portfolio:latest
    container_name: nuxt-portfolio-prod
    ports:
      - "3001:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
